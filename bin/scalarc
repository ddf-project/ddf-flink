def time[A](a: => A) = {
   val now = System.nanoTime
   val result = a
   val micros = (System.nanoTime - now) / 1000
   println("%d microseconds".format(micros))
   result
}

import _root_.io.ddf.flink.FlinkDDFManager

val dm = new FlinkDDFManager()

